% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-kernels.R
\name{get_gabor_kernel}
\alias{get_gabor_kernel}
\title{Generate a Gabor filter kernel}
\usage{
get_gabor_kernel(
  ksize,
  sigma,
  theta,
  lambda,
  gamma,
  psi = pi/2,
  kdepth = "CV_64F"
)
}
\arguments{
\item{ksize}{Length-2 integer vector \code{c(width, height)}. Both must be
positive odd integers.}

\item{sigma}{Single positive numeric. Standard deviation of the Gaussian
envelope.}

\item{theta}{Single numeric. Orientation of the filter normal in
\strong{degrees} (converted to radians internally).}

\item{lambda}{Single positive numeric. Wavelength of the sinusoidal
component, in pixels.}

\item{gamma}{Single positive numeric. Spatial aspect ratio. Values less
than 1 produce elongated filters; \code{1} gives a circular envelope.}

\item{psi}{Single numeric. Phase offset in radians. Default \code{pi / 2}.}

\item{kdepth}{Character. Precision of the returned kernel matrix:
\code{"CV_32F"} (single-precision float) or \code{"CV_64F"}
(double-precision, default).}
}
\value{
A numeric matrix of dimensions \code{height x width}.
}
\description{
Returns a numeric matrix containing Gabor filter coefficients, suitable for
use as \code{kernel} in \code{$filter2D()}.
}
\examples{
k <- get_gabor_kernel(c(9L, 9L), sigma = 2, theta = 0, lambda = 5, gamma = 0.5)
dim(k)
}
