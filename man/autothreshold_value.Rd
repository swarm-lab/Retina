% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threshold.R
\name{autothreshold_value}
\alias{autothreshold_value}
\title{Compute an automatic threshold value from an image}
\usage{
autothreshold_value(img, method, bins = 256L)
}
\arguments{
\item{img}{A single-channel \code{Image} object.}

\item{method}{A lowercase string naming one of the 17 supported methods:
\code{"imagej"}, \code{"huang"}, \code{"huang2"}, \code{"intermodes"},
\code{"isodata"}, \code{"li"}, \code{"maxentropy"}, \code{"mean"},
\code{"minerrori"}, \code{"minimum"}, \code{"moments"}, \code{"otsu"},
\code{"percentile"}, \code{"renyientropy"}, \code{"shanbhag"},
\code{"triangle"}, \code{"yen"}.}

\item{bins}{Integer >= 2. Histogram bin count used when the image depth is
not \code{CV_8U}. Ignored for \code{CV_8U} images (always 256 bins).
Default \code{256}.}
}
\value{
A single numeric (double) — the threshold in the image's native
  intensity units.
}
\description{
Returns the numeric threshold value computed by one of the 17 ImageJ Auto
Threshold algorithms, without modifying the image.
}
